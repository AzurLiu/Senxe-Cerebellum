# Archived Senxe experiments

These files preserve superseded experimental paths for research history. They
are not part of the audited CL1 application or confirmatory evidence path.

## Contents

- `senxe_demo.py`: historical direct wetware-control demonstration;
- `docs/HYBRID_RESIDUAL_V1.md`: retired single-axis residual design;
- `docs/CL1_PROTOCOL_V1.md`: superseded protocol record.

The only supported public entry point is:

```bash
python senxe_demo_robosuite.py
```

Archived code may depend on older SDK behavior and is not covered by the
current application-readiness claim.
